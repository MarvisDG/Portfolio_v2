import ExperienceSection from "@/Components/ExperienceSection";

export const metadata = { title: 'Resume - SamurAI' };
export default function Resume() {
  return <ExperienceSection />; // Reuse + PDF embed
}
